% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_request_helpers.R
\name{okx_paginate}
\alias{okx_paginate}
\title{Paginate a cursor-based OKX endpoint}
\usage{
okx_paginate(
  fetch_page,
  cursor_column,
  cursor = NULL,
  cursor_param = "after",
  max_pages = 100L,
  dedupe_by = cursor_column,
  ...
)
}
\arguments{
\item{fetch_page}{Function. A wrapper or function that returns one data frame
and accepts the cursor argument named by `cursor_param`.}

\item{cursor_column}{Character. Response column containing the cursor for the
next page, such as `"billId"` or `"ordId"`.}

\item{cursor}{Character or `NULL`. Initial cursor value.}

\item{cursor_param}{Character. Name of the request argument that receives the
cursor. Defaults to `"after"`.}

\item{max_pages}{Positive integer. Maximum pages to request.}

\item{dedupe_by}{Character vector or `NULL`. Columns used to remove duplicate
rows. Defaults to `cursor_column`; set `NULL` to retain duplicates.}

\item{...}{Additional arguments passed to `fetch_page` on every request.}
}
\value{
A `data.table` containing combined pages. `NULL` is returned when the
  first page is empty.
}
\description{
Repeatedly call an existing OKX GET wrapper that accepts a cursor parameter,
combining pages in retrieval order and removing duplicate rows by key.
}
\details{
Pagination stops on an empty page or missing cursor value. It errors if the
endpoint repeats a cursor, preventing an accidental infinite request loop.
}
